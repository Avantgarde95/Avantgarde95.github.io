---
import type { JSX } from "astro/jsx-runtime";
import ArrowUpRight from "@lucide/astro/icons/arrow-up-right";
import SmartLink from "@/shared/components/SmartLink.astro";

type Props = JSX.IntrinsicElements["a"];

const { class: className, ...others } = Astro.props;
---

<SmartLink class={`text-blue hover:text-yellow ${className}`} {...others}>
  <slot /><ArrowUpRight class="mb-1.5 inline-block" size="1rem" />
</SmartLink>

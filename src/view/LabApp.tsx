/** @jsx jsx */

import {jsx} from '@emotion/core';

export const LabApp = () => (
    <div css={{
        width: '100%',
        height: '100%',
        backgroundColor: '#dddddd'
    }}>

    </div>
);
